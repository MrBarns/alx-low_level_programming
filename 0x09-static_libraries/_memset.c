#include "main.h"

/**
 * _memset - empty function
 *
 * @s: empty var
 * @b: empty var
 * @n: empty var
 *
 * Return: empty pointer
 */

char *_memset(char *s, char b, unsigned int n)
{
	return (s);
}
